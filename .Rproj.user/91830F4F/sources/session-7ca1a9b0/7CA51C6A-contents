#' @title Add Two Numbers

#' @description This function takes two numeric inputs, \code{i} and \code{j}, and returns their sum.

#' @param i A numeric value representing the first number.

#' @param j A numeric value representing the second number.

#' @return A numeric value equal to the sum of \code{i} and \code{j}.

#' @export
add <- function(i, j) {
  return(i + j)
}
